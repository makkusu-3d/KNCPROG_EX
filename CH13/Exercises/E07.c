/**
 * Suppose that str is an array of characters. Which one of
 * the following statements is not equivalent to the other
 * three?
 *
 * (a) *str = 0;
 * (b) str[0] = '\0';
 * (c) strcpy(str, "");
 * (d) strcat(str, "");
 *
 * (d) Is the non equivalent one since is concatenates
 *     the null character to the end of a string
 */


