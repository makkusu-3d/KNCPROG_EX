/* Use typedef to create types named Int8, Int16, 
 * and Int32. Define the types so that they
 * represent 8-bit, 16-bit, and 32-bit integers
 * on your machine
 */
 
#include <stdint.h>

typedef int8_t  Int8;
typedef int16_t Int16;
typedef int32_t Int32;
