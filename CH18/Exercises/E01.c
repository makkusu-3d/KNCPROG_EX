/**
 * For each of the following declarations, identify the
 * storage class, type qualifiers, type specifiers,
 * and initializers
 *
 * (a)  static          char**          lookup(int level)
 *      storage class   type specifier  declarator
 *
 * (b)  volatile        unsigned long   io_flags;
 *      type qualifier  type specifier  declarator
 *
 * (c)  extern          char            *file_name[MAX_FILES], path[];
 *      storage class   type specifier  declarator
 *
 * (d)  static          const           char            token_buf[] = "";
 *      storage class   type qualifier  type specifier  declarator  initializer
 */

