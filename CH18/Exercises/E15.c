/**
 * Which property of a variable determines whether or not it has
 * a default initial value?
 *
 * (a) Storage duration
 * (b) Scope
 * (c) Linkage
 * (d) Type
 *
 * ANS: (a)
 *
 * "The first initialization of that variable is the zero-initialization
 *  of objects with static storage duration that happens before any
 *  other initialization takes place"
 */
