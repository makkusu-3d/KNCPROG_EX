/*
 * Why is it not a good idea for an identifier
 * to contain more than one adjacent underscore
 * as in current__balance, for example?
 *
 * The double underscore could be misinterpreted
 * as a single underscore creating an error in the
 * program
 */
