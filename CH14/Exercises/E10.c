/**
 * Function can often--but not always--be written as
 * parametrized macros. Discuss what characteristics
 * of a function would make it unsuitable as a macro.
 *
 * If the function implements recursion, requires debugging
 * and requires smaller binary code.
 */
